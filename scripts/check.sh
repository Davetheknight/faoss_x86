if [ -d "intermediates" ]; then echo "intermediates exists... good."; else mkdir intermediates && echo "made intermediates folder"; fi
if [ -d "kernel" ]; then echo "kernel folder exists... good."; else mkdir kernel && echo "made kernel folder- you're welcome :)"; fi
